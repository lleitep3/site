{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Car list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Brand</th>
                <th>Color</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('car_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.brand }}</td>
                <td>{{ entity.color }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('car_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
